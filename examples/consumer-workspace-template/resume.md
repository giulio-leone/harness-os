# RESUME PLACEHOLDER

Replace this file with the canonical asset or summary that downstream execution should use.

If your workflow needs a PDF or DOCX artifact, either add that real file to the copied workspace or update the source-context references accordingly.
